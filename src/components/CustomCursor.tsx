// Custom cursor removed — using native OS cursor.
// This component is kept as a shell to avoid import errors.
export default function CustomCursor(_props: { cursorHovered: boolean; cursorViewText: string }) {
  return null;
}
